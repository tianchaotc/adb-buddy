{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window. Grants the IPC permissions the React frontend needs to invoke Tauri commands, emit/listen to events (logcat streaming), and render the window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "core:image:default"
  ]
}
